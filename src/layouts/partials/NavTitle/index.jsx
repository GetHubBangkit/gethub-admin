export default function NavTitle({ title }) {
	return (
		<div className="mb-2 mt-4 px-6">
			<p className="block text-xs font-medium uppercase tracking-tight text-primary-200">{title}</p>
		</div>
	);
}
